import styled from 'styled-components/native'

export const Container = styled.View`
  flex: 1;
  background-color: ${(props) => props.theme.COLORS.COLGRAY_600ORS};

  align-items: center;
  justify-content: center;
`
